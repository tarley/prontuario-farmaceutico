<%= form_for(@sfc) do |f| %>
  <% if @sfc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sfc.errors.count, "error") %> prohibited this sfc from being saved:</h2>

      <ul>
      <% @sfc.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
