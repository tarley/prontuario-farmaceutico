<%= form_for(@ub) do |f| %>
  <% if @ub.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ub.errors.count, "error") %> prohibited this ub from being saved:</h2>

      <ul>
      <% @ub.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Descrição %>
    <%= f.text_field :description %>
  </div>
  
  <div class="actions">
    <%= f.submit :Salvar, :class => 'btn btn-success' %>
  </div>
<% end %>
